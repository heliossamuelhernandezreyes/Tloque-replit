## Packages
localforage | Required for PWA offline caching of book texts
framer-motion | Required for smooth animations of the search bar and UI elements

## Notes
PWA / Offline mode: Books are cached in IndexedDB via localforage upon fetch.
Animations: Framer motion handles the 3-second long press search reveal.
