name: Audio performance check

on:
  pull_request:
    paths:
      - 'client/src/audio/**'
      - 'shared/audio.ts'
      - 'shared/instrument-manifest.ts'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/audio-performance-check.yml'
  push:
    branches:
      - agent/performance-engine-v1
    paths:
      - 'client/src/audio/**'
      - 'shared/audio.ts'
      - 'shared/instrument-manifest.ts'
      - 'tests/**'
      - 'package.json'
      - '.github/workflows/audio-performance-check.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package.json

      - name: Install dependencies
        run: npm install --ignore-scripts

      - name: TypeScript
        run: npm run check

      - name: Tests
        run: npm test

      - name: Build
        run: npm run build
